#include <iostream>
using namespace std;

int main() {
    int n,m=0;
    cin >> n >> m;
    int h[n];
    int first[m],next[m];
    for(int i=0; i<n; i++){
        cin >> h[i]; 
    }

    for(int i=0; i<m; i++){
        cin >> first[i] >> next[i]; 
    }
}
